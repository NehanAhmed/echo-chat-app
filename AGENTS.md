# AGENTS.md — echo-chat-app

Greenfield MERN (MongoDB, Express, React, Node.js) chat application.

## Stack

| Layer | Tool |
|---|---|
| Frontend | React |
| Backend | Express / Node.js |
| Database | MongoDB |
| Runtime | Node.js |

## Project status

No files committed yet. The expected layout is a monorepo with separate `client/` and `server/` packages. This file should be updated once the structure, scripts, and conventions are established.

## Commands (to be filled)

Once `package.json` files exist, populate:

```
npm run dev          # start both client and server in dev mode
npm run test         # full test suite
npm run lint         # linter
npm run typecheck    # type checker if applicable
```

## Conventions (to be confirmed)

- Package manager: npm (default until a lockfile proves otherwise)
- No existing instruction files, CI config, or pre-commit hooks
- Verify client/server test runner (Jest vs Vitest) and MongoDB test setup before writing tests
